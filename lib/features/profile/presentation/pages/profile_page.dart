// ============================================================
// profile_page.dart
// PURPOSE: Stub — Phase 8 will fill this with:
//   - User avatar + name + email
//   - Monthly attendance stats (Present / WFH / Absent / %)
//   - Edit profile
//   - Logout button
// ============================================================

import 'package:flutter/material.dart';

class ProfilePage extends StatelessWidget {
  const ProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Profile')),
      body: const Center(
        // Phase 8: Replace with stats + user info UI
        child: Text('Profile — Phase 8'),
      ),
    );
  }
}
